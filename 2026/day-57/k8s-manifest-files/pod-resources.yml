apiVersion: v1
kind: Pod
metadata:
  name: resource-demo
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:                         #minimum guaranteed
        cpu: "100m"
        memory: "128Mi"
      limits:                           #maximum allowed
        cpu: "250m"
        memory: "256Mi"
