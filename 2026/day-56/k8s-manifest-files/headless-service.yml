apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
spec:
  clusterIP: None   #THIS makes it headless Also this line of code disables load balancing
  selector:
    app: web      #must match StatefulSet pod labels
  ports:
  - port: 80
    name: http
