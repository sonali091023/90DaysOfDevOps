# Day 07 – Linux File System Hierarchy & Scenario-Based Practice**

-->This task will help you understand the Linux file system, which is essential for Linux administration and DevOps. Follow these steps.

**Part 1: Linux File System Hierarchy**
1. /: 
**Understanding where things live in Linux helps in faster debugging, automation, and incident response.**

**/[root directory]** -->The / directory is the top-most directory in Linux, Everything in Linux starts from /.

<img width="1381" height="945" alt="image" src="https://github.com/user-attachments/assets/5d9665c1-d377-4996-868a-19f49a4bd305" />

-->Observed: Directories like bin, sbin, etc, home, var, usr, boot etc

-->I would use this when: Navigating the system or understanding disk layout issues.

2. home: 

**/home** -->Contains home directories for all normal users, Each user gets a personal folder here. User-specific files, configs, scripts live here.

<img width="1442" height="382" alt="image" src="https://github.com/user-attachments/assets/b0c3b35c-72e2-47e6-9db9-88890374b918" />

**Observed:** User directories like /home/ubuntu

-->I would use this when: Checking user files, scripts, or permission issues.

3. root:

**/root** -->Home directory for the root user. Only accessible by root.

<img width="1551" height="215" alt="image" src="https://github.com/user-attachments/assets/44265d84-80fe-4167-9f50-29fec1217427" />

**Observed:** Root-owned files and configs

-->I would use this when: Logged in as root during critical admin tasks.

4. etc:

**/etc** -->Contains system-wide configuration files. No binaries, Almost every Linux service keeps its configuration here.

<img width="1477" height="976" alt="image" src="https://github.com/user-attachments/assets/e85f207e-f21e-4901-bf97-73ecdc7f3942" />

**Observed:** Files like hostname, hosts, ssh

-->I would use this when: Debugging service configuration issues.

5. /var/log:

**/var/log** -->Stores logs generated by services and the system. One of the most important directories for DevOps.

<img width="1617" height="970" alt="image" src="https://github.com/user-attachments/assets/a1a1fbbd-9892-4095-bd99-45fb69ee5a15" />

**Observed:** Logs for syslog, nginx, auth.log

-->I would use this when: Investigating crashes, errors, or incidents, services like SSH, networking, or system settings

6. /tmp:

**/tmp** --Temporary files. Usually cleared on reboot.

<img width="1747" height="215" alt="image" src="https://github.com/user-attachments/assets/fe75f4e1-2aba-4bbd-81ef-2de96397ecc0" />

**Observed:** Temporary runtime files

-->I would use this when: Creating temporary test files or scripts.

7. /bin:

**/bin** -->Essential system binaries (ls, cp, mv), OR Contains essential system commands required during boot and recovery.

<img width="1472" height="967" alt="image" src="https://github.com/user-attachments/assets/958fdab6-260b-4ea5-8ebb-c9f78fc4e872" />

-->I would use this when: Running basic commands even in recovery mode.

8. /usr/bin:

**/usr/bin** --> User-level binaries installed by the OS or packages.

<img width="1347" height="990" alt="image" src="https://github.com/user-attachments/assets/58fb6650-446e-4155-b3db-ee117f0a238c" />

-->I would use this when: Working with installed tools like curl, git, docker.

9. opt:

**/opt** --> Optional or third-party applications.

<img width="810" height="225" alt="image" src="https://github.com/user-attachments/assets/24bc4535-8b07-4785-bcf2-cf8787fe0298" />

--> I would use this when: Managing custom or vendor applications.

**Hands-on task:**

-->Find the largest log file in /var/log: du -sh /var/log/* 2>/dev/null | sort -h | tail -5

<img width="1045" height="222" alt="image" src="https://github.com/user-attachments/assets/cd5e064a-8082-4962-8680-519291486494" />

**Explanation:**
- du -sh → Shows the size of each file/directory.
- 2>/dev/null → Hides permission errors.
- sort -h → Sorts sizes in human-readable order.
- tail -5 → Displays the five largest entries.

-->View the Hostname Configuration: cat /etc/hostname

<img width="821" height="100" alt="image" src="https://github.com/user-attachments/assets/623a6100-2cae-404b-9c8b-0efff5d2d8c4" />

-->Check Your Home Directory: ls -la ~

<img width="1307" height="972" alt="image" src="https://github.com/user-attachments/assets/d9b4a16d-2448-454e-8e4d-510ae8a17c28" />

Explanation:
- -l → Long listing format.
- -a → Includes hidden files (those beginning with .).
- ~ → Refers to your current user's home directory.

Submission Template:
<img width="960" height="561" alt="image" src="https://github.com/user-attachments/assets/4fbc43dc-bcfc-4888-b9d2-203147d070af" />

# Part 2: Scenario-Based Practice

**Important:** Focus on understanding the troubleshooting flow, not memorizing commands. Use the hints!

---

# SOLVED EXAMPLE: Understanding How to Approach Scenarios

### Example Scenario: Check if a service is running

**Question:** How do you check if the `nginx` service is running?

### Step 1:

```bash
systemctl status nginx
```

**Why:** Checks whether the service is **active**, **failed**, or **stopped**.

### Step 2:

```bash
systemctl list-units --type=service
```

**Why:** Lists all running services to confirm whether the service exists.

### Step 3:

```bash
systemctl is-enabled nginx
```

**Why:** Checks whether the service is configured to start automatically during system boot.

### Step 4:

```bash
journalctl -u nginx -n 50
```

**Why:** Displays the last 50 log entries for the service to identify startup or runtime issues.

### What I Learned

* Always start by checking the service status.
* If the service has failed, inspect the logs.
* Verify whether the service is enabled at boot.
* Follow a logical troubleshooting flow instead of guessing commands.

---

# Scenario 1: Service Not Starting

**Problem:** `myapp` failed to start after a reboot.

### Step 1:

```bash
systemctl status myapp
```

**Why:** Checks whether the service is active, failed, or stopped.

### Step 2:

```bash
journalctl -u myapp -n 50
```

**Why:** Displays the latest logs to identify the reason for the failure.

### Step 3:

```bash
systemctl is-enabled myapp
```

**Why:** Verifies whether the service is configured to start automatically during boot.

### Step 4:

```bash
systemctl list-units --type=service | grep myapp
```

**Why:** Confirms that the service exists and is recognised by systemd.

---

# Scenario 2: High CPU Usage

**Problem:** The application server is running slowly.

### Step 1:

```bash
top
```

**Why:** Shows live CPU and memory usage and identifies high CPU-consuming processes.

### Step 2:

```bash
ps aux --sort=-%cpu | head -10
```

**Why:** Lists the top 10 processes sorted by CPU usage.

### Step 3:

```bash
ps -p <PID> -o pid,pcpu,pmem,cmd
```

**Why:** Displays detailed information about the specific process consuming high CPU.

---

# Scenario 3: Finding Service Logs (Docker)

**Problem:** A developer wants to see the logs for the Docker service.

### Step 1:

```bash
systemctl status docker
```

**Why:** Confirms the current status of the Docker service and provides recent log hints.

### Step 2:

```bash
journalctl -u docker -n 50
```

**Why:** Displays the last 50 log entries for Docker.

### Step 3:

```bash
journalctl -u docker -f
```

**Why:** Follows the logs in real time while troubleshooting.

---

# Scenario 4: File Permission Issue

**Problem:** `/home/user/backup.sh` returns **"Permission denied"**.

### Step 1:

```bash
ls -l /home/user/backup.sh
```

**Why:** Checks the current file permissions.

### Step 2:

```bash
chmod +x /home/user/backup.sh
```

**Why:** Grants execute permission to the script.

### Step 3:

```bash
ls -l /home/user/backup.sh
```

**Why:** Verifies that the executable permission has been added.

### Step 4:

```bash
./backup.sh
```

**Why:** Runs the script to confirm that the permission issue has been resolved.

---

## Key Troubleshooting Flow

### Service Issues

1. Check service status.
2. Read service logs.
3. Verify boot configuration.
4. Confirm the service exists.

### Performance Issues

1. Monitor live resource usage.
2. Identify the top resource-consuming process.
3. Investigate the specific process using its PID.

### Log Investigation

1. Check service status.
2. View recent logs.
3. Follow logs in real time while reproducing the issue.

### Permission Problems

1. Check file permissions.
2. Grant the required permissions.
3. Verify the changes.
4. Test again.


